<%= form_with(model: mission, local: true) do |form| %>
  <% if mission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mission.errors.count, "error") %> prohibited this mission from being saved:</h2>

      <ul>
      <% mission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :Tarefa %>
    <%= collection_select(:mission,:task_id,@tasks,:id,:descricao) %>
  </div>

  <div class="field">
    <%= form.label :Organizador %>
    <%= collection_select(:mission,:organizer_id,@organizers,:id,:nome) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
